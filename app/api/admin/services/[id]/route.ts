import {NextResponse} from "next/server";import {db,requireAdmin} from "@/lib";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();const {id}=await params;const d=await req.json();const data:any={};if(d.name!==undefined)data.name=String(d.name).slice(0,100);if(d.description!==undefined)data.description=String(d.description).slice(0,300);if(d.price!==undefined)data.price=Math.max(0,Math.trunc(Number(d.price)));if(d.active!==undefined)data.active=!!d.active;return NextResponse.json({service:await db.service.update({where:{id},data})})}catch{return NextResponse.json({error:"Não foi possível atualizar"},{status:400})}}
export async function DELETE(req:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();const {id}=await params;await db.service.delete({where:{id}});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:"Não foi possível excluir"},{status:400})}}
