import {NextResponse} from "next/server";import bcrypt from "bcryptjs";import {db,createSession} from "@/lib";
export async function POST(req:Request){try{const {name,email,password}=await req.json();if(!name||!email||!password||password.length<6)return NextResponse.json({error:"Preencha os campos. Senha mínima de 6 caracteres."},{status:400});const exists=await db.user.findUnique({where:{email:email.toLowerCase()}});if(exists)return NextResponse.json({error:"E-mail já cadastrado."},{status:409});const passwordHash=await bcrypt.hash(password,12);const u=await db.user.create({data:{name,email:email.toLowerCase(),passwordHash}});await createSession(u.id);return NextResponse.json({ok:true})}catch{return NextResponse.json({error:"Erro ao cadastrar."},{status:500})}}
