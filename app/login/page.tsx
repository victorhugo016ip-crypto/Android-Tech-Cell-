 "use client";
import {useState} from "react"; import {useRouter} from "next/navigation";
export default function Login(){const [register,setRegister]=useState(false);const [name,setName]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [msg,setMsg]=useState("");const router=useRouter();
async function go(){setMsg("");const url=register?"/api/auth/register":"/api/auth/login";const body=register?{name,email,password}:{email,password};const r=await fetch(url,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const d=await r.json();if(!r.ok){setMsg(d.error||"Erro");return}router.push("/dashboard")}
return <main className="wrap"><div className="card" style={{maxWidth:460,margin:"70px auto"}}><div className="logo">ANDROID TECH CELL</div><h2>{register?"Criar conta":"Entrar"}</h2>{register&&<input placeholder="Nome" value={name} onChange={e=>setName(e.target.value)}/>}<input placeholder="E-mail" type="email" value={email} onChange={e=>setEmail(e.target.value)}/><input placeholder="Senha" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{msg&&<p className="error">{msg}</p>}<button className="btn" onClick={go}>{register?"Cadastrar":"Entrar"}</button><button className="btn alt" onClick={()=>setRegister(!register)}>{register?"Já tenho conta":"Criar conta"}</button></div></main>}
